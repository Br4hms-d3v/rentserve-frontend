import { SearchMaterialPipe } from './search-material.pipe';

describe('SearchMaterialPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchMaterialPipe();
    expect(pipe).toBeTruthy();
  });
});
